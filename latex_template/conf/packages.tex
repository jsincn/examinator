% Load additional packages here. This may be easier than replacing exam.tex

\usepackage{multicol}
\usepackage[%
	binary-units,
	per-mode=symbol,
]{siunitx}
\iflanguage{ngerman}{%
	\sisetup{locale=DE}
}{%
	\sisetup{locale=US}
}

